# Copy to client/.env.local for local development.
# Use the project's HTTPS URL and browser-safe sb_publishable_ key.
# Never put a Supabase secret key or service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
